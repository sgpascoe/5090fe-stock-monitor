services:
  - type: worker
    name: nvidia-stock-monitor
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python nvidia_stock_monitor.py
    plan: free
    envVars:
      - key: CHECK_INTERVAL
        value: 15
    # Set PUSHOVER_TOKEN and PUSHOVER_USER as environment variables in Render dashboard

